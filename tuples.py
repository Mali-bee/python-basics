# Tuples () - a data structure
# ordered, un-changeable, allows duplicates

learng_tuples = (18, 24, 5, 360, 6, 10)
print(learng_tuples)
print(type(learng_tuples))
print()

turple_randos = ("xyz", 18, False, 69, "SHAKA")     # turples can contain different data types
print(turple_randos)
print()

single_tuple = ("Mr Incredible",)                   # to create a tuple with only one item,make sure there is a comma there after the item, therwise Python won't recognise it as a tuple.
print(single_tuple)
print(type(single_tuple))
print()
